#include <stdio.h>
int main() {
    int num = 20;
    if (num > 0)
    {
        if (num % 2 == 0)
        {
            printf("Positive Even Number\n");
        }
        else
        {
            printf("Positive Odd Number\n");
        }
    }
    return 0;
}
