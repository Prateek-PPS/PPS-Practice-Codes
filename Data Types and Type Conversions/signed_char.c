#include <stdio.h>

int main() {
    signed char a = 'p';   // minimum value
    signed char b ='q';    // maximum value

    printf("----------------------a = %d\n", a);
    printf("----------------------b = %d\n", b);

    return 0;
}
