#include <stdio.h>
int main() {
    int num = -5;
    if (num >= 0)
    {
        printf("Number is positive\n");
    }
    /* else
    {
        printf("Number is negative\n");
    } */
    return 0;
}
